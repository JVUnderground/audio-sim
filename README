audio-sim: Audio environment simulator for testing source localisation algorithms.

To run audio-sim, insert the following to the command line:
ant compile jar run

Or directly, if already compiled:
ant run
